namespace Opus.Engine.Consumer.Assets;

/// <summary>Host-side role an asset will fill when resolved through a consumer catalog.</summary>
public enum ConsumerAssetRole
{
    /// <summary>Primary model rendered by the alpha scene bridge.</summary>
    PrimarySceneModel = 0,
}
